include ../mixins/_featProjectCard

section.feat-projects#feat-projects
  .section-title.m-bot-md.p-top-lg featured projects 
  .feat-projects__content
    if projects[0]
      +featProjectCard(projects[0])
    if projects[1]
      .card-divider
      +featProjectCard(projects[1])
    if projects[2]
      .card-divider
      +featProjectCard(projects[2])
